# Зависимости для разработки и тестирования
# Установка: pip install -r requirements-dev.txt

# Включаем основные зависимости
-r requirements.txt

# --- Тестирование ---

# Фреймворк для тестов
pytest>=7.4.0

# Отчёт о покрытии кода тестами
pytest-cov>=4.1.0

# Мокирование HTTP-запросов - позволяет тестировать клиент без реального API
responses>=0.25.0

# --- Качество кода ---

# Линтер + форматтер: быстрее flake8+black, всё в одном
ruff>=0.1.0

# --- Проверка типов ---

# Статическая проверка типов по аннотациям (mypy)
mypy>=1.8.0

# Тайп-стабы для библиотеки requests (нужны mypy для строгого режима)
types-requests>=2.31.0
